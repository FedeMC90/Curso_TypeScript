(() => {
    // Tuplas: Puedo definir qué tipo de dato tiene cada posición del array. 
    // Puedo definir tantas posiciones como quiera
    const hero: [string, number] = ['Dr Strange', 100];

    hero[0] = 'Ironman'; 
    hero[1] = 50;
    
})()